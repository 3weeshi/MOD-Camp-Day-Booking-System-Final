import { useState } from "react";
import { useLanguage } from "../components/LanguageContext.jsx";
function AssistantPage(){const {t}=useLanguage();const [question,setQuestion]=useState("");const [answer,setAnswer]=useState("Ask about booking, tent types, staff dashboard, or camp location.");const submit=e=>{e.preventDefault();const q=question.toLowerCase();if(q.includes("small"))setAnswer("Small Tent: suitable for small groups and does not include a private bathroom.");else if(q.includes("large"))setAnswer("Large Tent: suitable for families and includes a private bathroom.");else if(q.includes("vip"))setAnswer("VIP Tent: premium location and includes a private bathroom.");else if(q.includes("time")||q.includes("open"))setAnswer("The camp opens in the morning and closes at 11:00 PM.");else if(q.includes("staff"))setAnswer("Staff can confirm or cancel bookings from the Staff Dashboard.");else setAnswer("Login, go to Book, choose a tent from the map, fill the form, and submit your booking.");};return <section className="page-card auth"><h1>{t.assistantTitle}</h1><form className="simple-form" onSubmit={submit}><input value={question} onChange={e=>setQuestion(e.target.value)} placeholder="Ask a question"/><button type="submit">Ask</button></form><div className="assistant-answer">{answer}</div></section>;}
export default AssistantPage;
